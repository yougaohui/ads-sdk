pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// 移除 dependencyResolutionManagement 以兼容较旧的 Gradle 版本
// 直接在根项目中配置仓库

rootProject.name = "ads-sdk"
include ':ads-core'
include ':ads-cn'
include ':ads-global'
include ':ads-noop'
